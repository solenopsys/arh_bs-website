replicasCount: 1
fullName: "solenopsys-site"
selectorLabel: "solenopsys-site"

service:
  httpPort: 80

container:
  name: nginx
  image: registry.alexstorm.solenopsys.org/sc-fm-site
  pullPolicy: Always
