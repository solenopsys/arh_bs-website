replicasCount: 1
fullName: "solenopsys-site"
selectorLabel: "solenopsys-site"

service:
  httpPort: 80

container:
  name: nginx
  image: registry.richteri.solenopsys.org/sc-fr-website
  pullPolicy: Always
